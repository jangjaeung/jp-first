import { DynamicKey } from '@/components/types'


// eslint-disable-next-line @typescript-eslint/no-explicit-any
export const layoutComp: DynamicKey<any> = {
    // home: _lazy(() => import('./test')),
}
